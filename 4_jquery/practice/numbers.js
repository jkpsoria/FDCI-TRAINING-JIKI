// create 2 variables here

// calculation here
const firstNum = 10;
const secondNum = 20;

const add = (num1, num2) => num1 + num2;
const sub = (num1, num2) => num1 - num2;
const prod = (num1, num2) => num1 * num2;
const quo = (num1, num2) => num1 / num2;

console.log(add(firstNum, secondNum));
