const person = {
  name: "wew",
  age: 0,
};

// destructure the object here

const { name, age } = person;

console.log(name);
console.log(age);

const numbers = [1, 2, 3];

const [one, three, two] = numbers;

console.log(three);
