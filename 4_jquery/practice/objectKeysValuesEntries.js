// create object here
const person = {
  name: "jiki",
  age: 99,
};

// use Object.keys
const keys = Object.keys(person);
console.log(keys);

// use Object.values
const values = Object.values(person);
console.log(values);

// Object.entries
const entries = Object.entries(person);
console.log(entries);
